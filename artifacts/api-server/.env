# Local API server — see .env.example for all options.
# Do not commit real credentials to public repos.

PORT=8080
USE_MOCK_DATA=false

# Required when USE_MOCK_DATA=false
MONGODB_URI=mongodb+srv://skillsnap:skillsnap404@cluster0.prxjz.mongodb.net/?appName=Cluster0
MONGODB_DB=skillsnap

# Optional: Postgres → Mongo migration only
DATABASE_URL=

# Optional: set in production (defaults to a dev secret in code if unset)
# JWT_SECRET=
