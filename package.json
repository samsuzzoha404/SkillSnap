{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node tools/preinstall.cjs",
    "dev:admin": "pnpm --filter @workspace/admin dev",
    "verify:api": "pnpm --filter @workspace/scripts verify-api",
    "typecheck:stack": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server --filter @workspace/scripts --filter @workspace/skillsnap run typecheck",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "pnpm": {
    "overrides": {
      "@types/react": "~19.1.10",
      "@types/react-dom": "~19.1.7",
      "react": "19.1.0",
      "react-dom": "19.1.0"
    }
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
