{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed": "tsx --import ./src/loadEnv.ts ./src/seed.ts",
    "verify-api": "tsx --import ./src/loadEnv.ts ./src/verifyApi.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "bcryptjs": "*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/bcryptjs": "*",
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
