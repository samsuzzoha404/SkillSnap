# Workspace-level environment (optional)
#
# API server demo mode is configured in: artifacts/api-server/.env
# Expo app environment is configured in: artifacts/skillsnap/.env
#
# Keeping this file allows tools/CI to inject shared vars if desired.
