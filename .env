# Workspace root — optional.
# Seed / verify scripts merge this with artifacts/api-server/.env (see scripts/src/loadEnv.ts).
#
# Copy .env.example to .env if you need workspace-level overrides.

# Optional: override default http://localhost:8080/api for verify:api
# API_VERIFY_BASE_URL=http://localhost:8080/api
